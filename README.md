# A simple multiple rendering target demo with orx

This repo provides [code](src/Object.cpp) and [configuration](data/config/orx-mrt-demo.ini) demonstrating one way to use multiple rendering targets in [the orx game engine](https://orx-project.org/).
