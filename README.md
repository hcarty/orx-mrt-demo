# A simple multiple rendering target demo with orx

This repo provides code in [Label.cpp](src/Label.cpp) and [Object.cpp](src/Object.cpp), and configuration in [orx-mrt-demo.ini](data/config/orx-mrt-demo.ini) demonstrating one way to use multiple rendering targets in [the orx game engine](https://orx-project.org/).
